PFont f;
  int x = int(random(width));
  int y = int(random(height));
  int margin = 10;
  int counter = 0;
  int stop = 


void setup() {
  f = createFont("Georgia", 24);
  textFont(f);
  size(520, 695);
  frameRate (200);
}

void draw(){
    translate (margin*4, margin*4);
text ("Juliet", random (x), random (y));
fill (255, 51, 102);
if  (> 189){
  else (text(" "));
}
}
}
